#pragma once

#include "lexer/token.hpp"

// Recursive-descent parser for Folio (.folio files).

namespace folio {

// TODO: declare a Parser class here.
// construct with a token stream (from the Lexer), exposes
// a parse() method that returns the root Document AST node.

} 
